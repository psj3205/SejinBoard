<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>사용자 리스트 페이지</title>
  </head>
  <body>
    <h2>사용자 리스트</h2>
    <div>
      <ul>
        <% for(let i = 0; i < results.length; i++ ) { const curId =
        results[i]._doc.id; const curName = results[i]._doc.name; %>
        <li>#<%= i %> - 아이디 : <%= curId %>, 이름 : <%= curName %></li>
        <% } %>
      </ul>
    </div>
    <br /><br /><a href="/public/listuser.html">다시 요청하기</a>
  </body>
</html>
